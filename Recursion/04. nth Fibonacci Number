#include<bits/stdc++.h>
using namespace std;

int fibonacci(int n){
	if(n==0 || n==1){
		return n;
	}
	int prev_term=fibonacci(n-1);
	int prev_prev_term=fibonacci(n-2);
	return prev_term+prev_prev_term;
}
int main(){
	int n;
	cin>>n;
	cout<<fibonacci(n);
}
